defaults:
  - _self_
  - datapaths: datapaths
  - shapefiles: shapefiles
  - satellite_pm25: satellite_pm25

# == aggregation args
temporal_freq: monthly # annual, monthly to be matched with cfg.satellite_pm25
year: 2020
month: "01" # ignored if annual

# == shapefile download args
polygon_name: county # zcta, county to be matched with cfg.shapefiles
shapefile_year: 2020 #to be matched with cfg.shapefiles

# == satellite pm25 download args
download_path: "~/Downloads"
download_wait_time: 5 #seconds

hydra:
  run:
    dir: logs/${now:%Y-%m-%d}/${now:%H-%M-%S}
    